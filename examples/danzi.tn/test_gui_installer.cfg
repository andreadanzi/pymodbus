# pip install pynsist
# python -m nsist test_gui_installer.cfg
[Application]
name=test-gui
version=2.7
entry_point=test_gui:main

[Python]
version=2.7.12
bitness=64

[Include]
# Importable packages that your application requires, one per line
packages = gi
     matplotlib
     numpy
     pymongo
     pymodbus
     twisted

# Other files and folders that should be installed
files = test_gui.cfg
    test_gui.glade
#    data_files/